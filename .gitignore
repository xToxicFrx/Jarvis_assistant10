# Lokale Umgebungsvariablen / Secrets - niemals committen
.env
.env.*
!.env.example

# Vercel
.vercel

# Node (jsdom ist nur fuer den optionalen DOM-Test; Lockfile nicht committen -> keine Build-Abhaengigkeit)
node_modules/
package-lock.json

# Betriebssystem / Editor
.DS_Store
Thumbs.db
*.log
